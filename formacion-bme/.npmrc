# Este proyecto usa el registro público para evitar timeouts cuando el Nexus
# corporativo (NFQ) no es alcanzable (sin VPN o red distinta).
registry=https://registry.npmjs.org/
fetch-timeout=600000
fetch-retries=5
